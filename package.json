
{
  "name": "taka-tracker",
  "version": "1.0.0",
  "description": "Personal money tracking app for BDT",
  "main": "index.tsx",
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'Build step: Ensure all files are in the root directory for Capacitor'",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@google/genai": "^1.41.0",
    "lucide-react": "^0.463.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "recharts": "^3.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
